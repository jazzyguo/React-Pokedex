import React from 'react'

import PokemonDetails from 'components/PokemonDetails'

const PokemonIdPage = () => (
    <div>
        <PokemonDetails />
    </div>
)

export default React.memo(PokemonIdPage)
